<?xml version="1.0"?>
<project name="Travis" default="compile">
	<property name="builddir" location="bin" />

	<path id="Travis.classpath">
		<pathelement location="${builddir}"/>
	</path>

	<target name="init">
		<mkdir dir="${builddir}" />
	</target>

	<target name="compile" depends="init">
        <javac destdir="${builddir}" includeantruntime="false">
            <src path="src"/>
            <classpath refid="Travis.classpath"/>
        </javac>
	</target>

</project>
